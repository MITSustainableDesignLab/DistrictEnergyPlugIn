<UserControl x:Class="DistrictEnergy.DistrictControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:lvc="clr-namespace:LiveCharts.Wpf;assembly=LiveCharts.Wpf"
             xmlns:districtEnergy="clr-namespace:DistrictEnergy"
             xmlns:vm="clr-namespace:DistrictEnergy.ViewModels"
             mc:Ignorable="d"
             d:DesignWidth="450" d:DesignHeight="800" d:DataContext="{d:DesignData ResultsViewModel}">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="50*"/>
            <RowDefinition Height="50*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="100*"/>
        </Grid.ColumnDefinitions>
        <Button Grid.Row="2" Grid.Column="0" Click="RunSimulationClick" Height="20"></Button>
        <lvc:CartesianChart Grid.Column="0" Grid.Row="0" DisableAnimations="True" Hoverable="False"
                            DataTooltip="{x:Null}" Series="{Binding StackedHeatingSeriesCollection}">
        </lvc:CartesianChart>
    </Grid>

</UserControl>